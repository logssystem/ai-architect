import { AuthForm } from '@/components/auth-form'

export const metadata = { title: 'Criar conta — AI Solutions Architect' }

export default function SignUpPage() {
  return <AuthForm mode="sign-up" />
}
